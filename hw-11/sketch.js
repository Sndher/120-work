//OOP use constructor method to set up properties for all objects
//at least 2 other methods that cause the objects to DO SOMETHING on screen

// in setup() function create objects and add them to an array
// in draw() function call appropriate methods on each object so they do something whatever the thing is that they do {
// use at code examples on ARRAY of objects
